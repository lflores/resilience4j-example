# Docker-specific configuration for Accounts API
spring:
  config:
    activate:
      on-profile: docker

# Docker networking configuration
server:
  address: 0.0.0.0

management:
  server:
    address: 0.0.0.0